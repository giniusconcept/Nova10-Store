name: GitHub Actions Diagnostic

on:
  push:
    branches:
      - main

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Runner check
        run: echo "GitHub Actions runner is available"
